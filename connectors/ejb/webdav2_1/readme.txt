Use run.sh or run.nat in this directory for simple test when ear deployed at the same machine where client running.

JCR path can be set:

sh run.sh --jcr-path=<jcr_path>

for example

sh run.sh --jcr-path=/jcr/repository/collaboration
